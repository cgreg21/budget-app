# dependencies (also holds the generated .node-gtk-types/ TypeScript types,
# which are regenerated on install — never commit them)
node_modules/
# build output
dist/
build/*

# test coverage reports
coverage/

# misc
*.log
.DS_Store
*.AppImage
build/appimage
Budget-App-windows-x64/
Budget-App-windows-x64.zip
