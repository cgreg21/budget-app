/*
 * i18n/index.ts — barrel for the locale detection and the translations.
 */
export { DEFAULT_LOCALE, getLocale, type Locale } from './locale.js'
export { t, type IconKey, type Strings } from './strings.js'
